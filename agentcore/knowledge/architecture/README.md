# Architecture Knowledge

Durable operating architecture for AgentCore systems.

- `system-architecture.md` is the primary map of current channels, workflows, data stores, deployments, and known constraints.

Update these pages whenever changing communication channels, workflows, runners, hosted endpoints, OAuth scopes, deployment targets, queue semantics, or durable knowledge locations.
